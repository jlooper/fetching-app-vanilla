<Page loaded="loaded">
    <ActionBar title="Fetching">
    </ActionBar>
    <StackLayout>
        <Image src="{{ photo }}" width="auto" height="200"/>
        <Label text="{{ 'Name: ' + name }}" textWrap="true"/>
        <Label text="{{ 'Breed: ' + breed }}" textWrap="true"/>
        <Label text="{{ 'Age: ' + age }}" textWrap="true"/>    
    </StackLayout>
</Page>